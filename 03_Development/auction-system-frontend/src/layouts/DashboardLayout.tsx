// Dashboard Layout
// TODO: Implement layout with Header + Sidebar + Content

const DashboardLayout = () => {
  return <div>Dashboard Layout - Header + Sidebar + Content</div>;
};

export default DashboardLayout;
